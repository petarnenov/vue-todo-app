node_modules/
dist/
coverage/
.DS_Store
.env
.env.*
.agent-orchestrator-runs/
plan.md
task.md

# MCP server configs carry Personal Access Tokens — never commit. Each line maps to one
# agent's HTTP-transport config (PAT lives in an Authorization: Bearer header inside).
.mcp.json
.vscode/mcp.json
.cursor/mcp.json
.codex/config.toml
.continue/mcpServers/
